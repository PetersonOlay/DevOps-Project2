# Provider and backend configuration for the root module.
# Terraform >= 1.10 is required for the S3 native lock file (use_lockfile = true).
# The backend "s3" block is intentionally empty — values are injected at init time:
#   terraform init -backend-config=environments/<env>.backend.hcl

terraform {
  required_version = ">= 1.10.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
    kubernetes = {
      source  = "hashicorp/kubernetes"
      version = "~> 2.27"
    }
    helm = {
      source  = "hashicorp/helm"
      version = "~> 2.13"
    }
    tls = {
      source  = "hashicorp/tls"
      version = "~> 4.0"
    }
    random = {
      source  = "hashicorp/random"
      version = "~> 3.6"
    }
  }

  backend "s3" {}
}
