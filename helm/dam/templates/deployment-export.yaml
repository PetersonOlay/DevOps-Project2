apiVersion: apps/v1
kind: Deployment
metadata:
  name: dam-export-worker
  namespace: {{ .Values.global.namespace }}
  labels:
    app: dam-export-worker
spec:
  replicas: {{ .Values.exportWorker.replicas }}
  selector:
    matchLabels:
      app: dam-export-worker
  template:
    metadata:
      labels:
        app: dam-export-worker
    spec:
      serviceAccountName: dam-export-worker
      containers:
        - name: dam-export-worker
          image: "{{ .Values.image.registry }}/dam/export-worker:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - secretRef:
                name: dam-secrets
          env:
            - name: NODE_ENV
              value: production
            - name: AWS_REGION
              value: {{ .Values.global.region }}
            - name: S3_BUCKET
              value: {{ .Values.api.s3Bucket }}
            - name: POLL_INTERVAL_MS
              value: {{ .Values.exportWorker.pollIntervalMs | quote }}
          resources:
            requests:
              cpu: {{ .Values.exportWorker.resources.requests.cpu }}
              memory: {{ .Values.exportWorker.resources.requests.memory }}
            limits:
              cpu: {{ .Values.exportWorker.resources.limits.cpu }}
              memory: {{ .Values.exportWorker.resources.limits.memory }}
