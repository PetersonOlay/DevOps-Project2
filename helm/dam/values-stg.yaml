# Staging environment overrides.
# Usage: helm upgrade dam ./helm/dam -f app/helm/dam/values-stg.yaml --reuse-values

global:
  namespace: dam-stg

api:
  replicas: 1
  s3Bucket: "dam-assets-395675597879-stg"

web:
  replicas: 2

transformWorker:
  replicas: 1
  pollIntervalMs: "5000"

exportWorker:
  replicas: 1
  pollIntervalMs: "5000"

image:
  pullPolicy: IfNotPresent
