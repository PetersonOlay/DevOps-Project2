apiVersion: apps/v1
kind: Deployment
metadata:
  name: dam-export-worker
  namespace: {{ include "dam.namespace" . }}
  labels:
    {{- include "dam.labels" . | nindent 4 }}
    app.kubernetes.io/component: export-worker
spec:
  replicas: {{ .Values.exportWorker.replicas }}
  selector:
    matchLabels:
      app: dam-export-worker
  template:
    metadata:
      labels:
        app: dam-export-worker
        {{- include "dam.labels" . | nindent 8 }}
    spec:
      serviceAccountName: dam-export-worker
      containers:
        - name: dam-export-worker
          image: {{ include "dam.image" (dict "root" . "name" "dam/export-worker") }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - secretRef:
                name: dam-secrets
          env:
            - name: NODE_ENV
              value: "production"
            - name: AWS_REGION
              value: {{ .Values.global.region | quote }}
            - name: S3_BUCKET
              value: {{ .Values.api.s3Bucket | quote }}
            - name: POLL_INTERVAL_MS
              value: {{ .Values.exportWorker.pollIntervalMs | quote }}
          resources:
            {{- toYaml .Values.exportWorker.resources | nindent 12 }}
